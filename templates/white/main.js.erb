define([
    'ace/theme'
], function(aceTheme) {
    var themes = codebox.require("core/themes");

    themes.add({
        id: "<%= id %>",
        title: "<%= title %>",

        editor: {
            'theme': aceTheme
        }
    });
});
